# ADR-005 — Server Authorization, RLS & Scoped Privileged Access
Status: ACCEPTED

Authorization follows Identity → Membership → Role/Capability → Resource Scope → Entitlement → Policy Decision. Server authority is mandatory; DB RLS is defense-in-depth. Privileged bypass is named, limited and audited.

Certification: T20 + integration + runtime RLS.
