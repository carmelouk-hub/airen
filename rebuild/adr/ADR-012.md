# ADR-012 — Secrets & Provider Credentials Outside Operational Data
Status: ACCEPTED

Secrets, webhook signing keys, OAuth tokens and provider credentials live in a secret manager/credential vault. Operational rows store only opaque credential references and non-secret provider identifiers.

Certification: secret scanning + access control + integration smoke.
