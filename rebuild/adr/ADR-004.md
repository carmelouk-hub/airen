# ADR-004 — Hostname → Tenant Resolution & Custom Domains
Status: ACCEPTED

Tenant.slug is stable logical identity. TenantDomain/CustomDomainRegistry resolves trusted hostname to tenant_id (+ optional location_id). Unknown non-empty hosts fail closed. Slug subdomains are accepted only under configured platform base domains.

Certification: domain/DNS integration + T20.
